# قبس (Qabas) — استوديو صناعة المحتوى الإسلامي بالذكاء الاصطناعي

> **قاعدة الحوكمة:** اقرأ **`AGENTS.md` بالكامل أولاً** قبل أي تعديل — هو الوثيقة الحاكمة (الحالة التشغيلية، الممنوعات، الخطوة التالية الوحيدة).
> أي عمل جوهري **يجب** أن يحدّث `AGENTS.md` و`README.md` معاً وإلا فالعمل غير مكتمل.

| | |
|---|---|
| الحزمة | `com.qabas.app`
| الإصدار | 1.2.1 (versionCode 3) — لوحة تحكم سحابية كاملة |
| SDK | minSdk 24 · target/compile 36 |
| التقنية | Kotlin 2.0.21 · Jetpack Compose (Material 3) |

---

## 1. ما هو قبس

**قبس** استوديو أندرويد متكامل لصناعة المحتوى الدعوي والإسلامي: من **فكرة** يكتبها المستخدم حتى **فيديو MP4** منتهٍ (كابشن عربي + تعليق صوتي + مونتاج سينمائي):

```
فكرة → فهم وتحليل (AI) → سكربت ومشاهد → B-Roll → صوت (TTS) → معالجة FFmpeg → مراجعة → حفظ/مشاركة
```

المعالجة تجري **محلياً على الجهاز عبر FFmpeg Kit**، والذكاء الاصطناعي يشغّل كتابة السكربت وهندسة المشاهد والتحليل — مع fallback مجاني عند غياب المفاتيح أو الشبكة.

---

## 2. الهوية البصرية واللغة

- **السمة الداكنة (الافتراضية):** Dark Slate + Gold
  - الخلفية العميقة `#0B0F19` · سطوح البطاقات `#151B2B`
  - الذهبي الأساسي `#E8C547` · الذهبي الثانوي `#F5D76E`
  - النصوص `#F8FAFC` / `#94A3B8`
- **السمة الفاتحة:** `#F8FAFC` بخلفيات بيضاء وسطحى `#F1F5F9`.
- **الخط:** Cairo (عربي/لاتيني مرن).
- **RTL كامل:** اللغة الافتراضية `ar` (تُحفظ في `SharedPreferences` تحت `app_language`)، مع موارد `values-ar`.
- **شاشة الدخول:** آية الدخول «إن إلى ربك الرجعى» (~7ث، fade in/out عبر `AudioPlayerManager`) + بوابة استوديو ذهبية (Lottie). **السبلاش أُزيل كلياً (سبتمبر 2026)** — لا شاشة سبلاش في أي مكان؛ الدخول يبدأ مباشرة من `DATA_LOADING`.

- **توقيع AI (سبتمبر 2026):** هوية تقنية بنفسجية مهيمنة — `primary` بنفسجي `#8B5CF6` + إكسنت سيان `#22D3EE` + توهج أزرق `#3B82F6` في السمتين (الفاتحة بنفسجي `#7C3AED` / سيان `#0891B2` / بنفسجي فاتح `#A78BFA`). **الذهبي محفوظ للمقدسات** (بطاقة التجويد، ترويسة الدخول التبركية، والعناوين التبركية).
- **شاشة الدخول (سبتمبر 2026):** ترويسة ترحيب «أهلاً بك في قبس / استوديو الإنتاج وصناعة الأثر» بالذهبي + تدرج العلامة (بنفسجي→أزرق→سيان) على زر الدخول وحقول/روابط بنفسجية/سيان. **كتلة اللوجو القديم أُزيلت مع حذف أصوله (سبتمبر 2026)** — البطاقة بانتظار اللوجو الجديد.

---

## 3. البنية التقنية

| الطبقة | التقنية |
|---|---|
| الواجهة | Jetpack Compose · Material 3 · Navigation عبر `AppState` (قائمة موحّدة من الحالات + `AnimatedContent` transition) |
| الحالة | ViewModel + StateFlow |
| قاعدة البيانات | Room (`qabas_database` v4): `projects` · `taste_profile` · `hadith_cards` · `audio_tracks` · `reel_scripts` · `series_records` |
| معالجة الفيديو | FFmpeg Kit (`ffmpeg-kit-full`) عبر `VideoProcessor` / `VideoEngineManager` / `MontageDirector` |
| التشغيل والوسائط | Media3 ExoPlayer · Coil · Lottie · OpenCV · CameraX |
| الذكاء الاصطناعي | Gemini 2.0-flash · Groq · تحليل محلي (`LocalImageAnalyzer`) |
| الصوت | Azure Speech TTS · ElevenLabs TTS · محرك نطق أندرويد (fallback مجاني دائماً) |
| الوسائط | Pexels · Pixabay (B-Roll) · HuggingFace (توليد صور) |
| السحابة | **Supabase** (نشطة) + **Firebase** (مسار اختياري) |
| المدفوعات | Billing Library 7.x (متجر/باقات/اشتراكات) |
| التخزين | Room + DataStore + SharedPreferences |

---

## 4. الشاشات والأقسام

التنقّل مركزي عبر `AppState` (أكثر من 40 حالة). المجموعات الأساسية:

- **حلقة التنقل الشمسية (سبتمبر 2026):** الشريط السفلي `QabasBottomNavigation` استُبدل (بلا أي حذف، ويبقى دون استخدام) بنظام «الكواكب حول الشمس» `QabasSolarSystemNavigation`: 5 كواكب بنفس الألوان/الترتيب (الرئيسية / بطاقة حديث / القرآن / ريلز / الإعدادات) تدور ببطء (8°/ث، تحترم `ANIMATOR_DURATION_SCALE`) حول شعلة ذهبية، والكوكب النشط مثبَّت عند المقعد السفلي (270°) ويكبر ويلمع بحد ذهبي؛ نقرة كوكب → حوار تأكيد («الدخول إلى …؟» / «تأكيد ✓» / «إلغاء») → تنقّل مع اهتزاز خفيف؛ حركة عبر `graphicsLayer` فقط وتجميد عند غير `RESUMED`. (إصلاح بناء: `compose.ui` المحلول 1.9.0 يضع `HapticFeedbackType` في `androidx.compose.ui.hapticfeedback` و`LocalHapticFeedback` في `androidx.compose.ui.platform`.)

- **مسار الإنتاج:** المدخل `INPUT` → `UNDERSTANDING` (تحليل الفكرة ومشاهد) → `RESOURCES` (B-Roll) → `PROCESSING` (FFmpeg مع تقدم خطي وأوقاف آمنة) → `REVIEW` → `SAVE_SHARE`.
- **الرئيسية والدراسة:** `HOME` (الرئيسية) · `LEADERBOARD` (المتصدرين والمكافآت) · `KNOWLEDGE_HUB` (طلب العلم + XP + سلاسل يومية) · `QURAN_HUB` · `HADITH_STUDIO` (بطاقات الحديث + أذكار يومية + إشعارات) · `CONTENT_GUARD`.
- **الصناعة القصيرة:** `REELS` (ريلز 9:16، هوكس، كابشن، هاشتاقات) · `TELEPROMPTER` (المُلقن الذكي) · `VIDEO_STYLE_CLONER` (استنساخ الأنماط البصرية) · `PHOTO_STUDIO`.
- **الاستوديو الذكي:** `STYLE_SELECTION` / `CREATE_STYLE_OBJECT` (عقل أسلوبي `StyleBrain` مع Radar Chart ومقارنات) · `SMART_DIRECTOR` (المخرج الذكي) · `TASTE_PROFILE`.
- **الحساب والنشر:** `LOGIN` / `REGISTER` · `PROFILE` · `SETTINGS` · `API_KEYS` (مفاتيح API وتصدير/استيراد نسخ احتياطي) · `NOTIFICATIONS` · نشر مباشر (`DirectPublisherDialog`) عبر حسابات التواصل.
- **المطور:** `DEVELOPER_DASHBOARD` · `API_DOCS`.
- **التجارب المعمارية:** `ENTERPRISE_PORTAL` · `YOUTUBE_STUDIO` · `REQUEST_CHAT` · `APP_IDEA_FORM` · `PREMIUM_UPGRADE` · `PAYMENT`.

---

## 5. الخدمات الذكية والسياسة المجانية

- **يعمل التطبيق بلا أي مفتاح:** التحليل المحلي + محرك النطق المدمج في أندرويد (TTS) + FFmpeg تعمل **دون إنترنت ودون مفاتيح**.
- **المفاتيح اختيارية** وتُدخل من شاشة «مفاتيح API» داخل الإعدادات، أو عبر ملف `.env` في البناء:
  - أنا Gemini (تحليل/سكربت/أنماط) · Groq (بديل أسرع)
  - Azure Speech (TTS) + المنطقة · ElevenLabs (TTS)
  - Pexels / Pixabay (B-Roll حقيقي) · HuggingFace (صور)
  - Supabase (URL + anon key)
- **مبدأ صارم:** لا حقن محتوى ديني خاطئ كـ fallback للصوت/النص، ولا `return true` بعد فشل صامت.
- **شاشة «مفاتيح API» (الحصاد شبه الآلي + الصحة الحيّة):**
  - **التقاط من الحافظة 📋:** يكتشف المفتاح تلقائياً بالنمط (Regex) عندما تنسخه، ويملأ الحقل ويحقّقه فوراً (عند اللصق أو عند التركيز على حقل فارغ)، مع أزرار «التقاط من الحافظة» و«افتح الموقع» للإنشاء — وهي صفّ كامل العرض **أسفل الحقل** (وليست داخل `trailingIcon` حتى لا تُسحق كباركود غير قابل للضغط).
  - **تحقق Gemini واضح:** المفتاح الذي لا يبدأ بـ `AIzaSy…` (مثل توكنات `AQ.…` الخاصة بـ OAuth) يُرفض برسالة عربية تفسّر السبب وتوجّه لإنشاء مفتاح من `aistudio.google.com/app/apikey` — لا أخطاء HTTP 400 مبهمة بعد الآن.
  - **شارة «🆓 بديل مجاني»:** على كل بطاقة بلا مفتاح، توضح البديل المحلي الذي يعمل مجاناً — الصدق أولاً، لا وهم.
  - **فحص صحة المفاتيح الحي 🩺:** لوحة ذاتية الاكتفاء تفحص 7 خدمات بأزمنة استجابة حقيقية (🟡 غير محدد / 🟢 متصل / 🔴 مرفوض) + «خطة إصلاح مقترحة 🛡️» + إجمالي مكالمات/نجاح حقيقي من `ApiUsageTracker`.

### متغيرات البيئة (`gradle` → `BuildConfig`)

قراءة المفاتيح تأتي من `.env` (جذر المشروع) عبر **secrets-gradle-plugin**. القوالب في `.env.example`.

> **تحذير:** قيمة المفتاح **لا تُترك فارغة** (`KEY=""`) — تسبب انكسار توليد `BuildConfig.java`. استخدم دائماً placeholder غير فارغ مثل `"your_key"`.

---

## 6. طبقة السحابة

- **Supabase = الطبقة النشطة حالياً** (URL `https://wyevrdnnttckaihhqxbl.supabase.co` + anon key في `.env` المحلي).
  - `SupabaseConfig.kt` محصّن ضد placeholder: أي APK بلا قيم حقيقية **يتدهور للوضع المحلي** بدل الاتصال بعنوان وهمي.
  - قراءة `users` · كتابة/حذف `transactions` · مشاريع المستخدمين — كلها مسارات حقيقية (تتحقق عبر `SupabaseServices` / `CloudServices`).
- **Firebase = مسار ثانوي/اختياري** (تحليل + Auth + Firestore + Analytics محترم لإعداد `analytics_enabled` في `qabas_prefs`).
- لوحة المطور لا تعرض بلوغاً كاذباً: متتبع الاستهلاك `ApiUsageTracker` فارغ عند البدء ويمتلئ من استدعاءات HTTP الفعلية فقط.

---

## 7. البناء والتحقق

```bash
./gradlew :app:compileDebugKotlin   # فحص سريع
./gradlew assembleDebug             # بناء APK
```

- **CI:** `.github/workflows/android-ci.yml` — build & sign، إصلاح `gradle-wrapper.jar` تلقائياً، و«**Inject secrets**»: يكتب `.env` من أسرار GitHub إن وُجدت (بدونها يبني بوضع محلي آمن). 📦 APK يرفع كـ Artifact `qabas-debug-apk` (احتفاظ 7 أيام).
- **بناء محلي مُتحقَّق:** `:app:assembleDebug` → BUILD SUCCESSFUL محلياً (Temurin JDK 17 + Android SDK platform 36 في `~/tooling`، `sdk.dir` في `local.properties`) — يثبت ربط كل الوظائف دون أخطاء ترجمة.
- **حارس الانهيارات:** `QabasCrashGuard` يلتقط أي استثناء JVM غير مُتصدّى له في أي Thread → ملف `filesDir/crash_logs/` + سطر في لوحة المطور بستاك كامل، وإعادة إطلاق تلقائية محمية ضد التكرار (نافذة 15 ثانية) للـ main thread.
- **التوقيع الثابت (إصلاح التحديث بلا حذف):** مفتاح PKCS12 ثابت مدمج في المستودع (`signing/qabas-ci.p12`، بيانات debug القياسية المعروفة علناً android/androiddebugkey — هوية تحديث وليس مفتاح نشر) — كل بناء debug/release موقّع به فيُثبَّت التحديث فوق النسخة القديمة مباشرة. (سابقاً كان مفتاح الـ runner يتولد من جديد كل بناء → توقيع مختلف → وجوب الحذف. النسخ المثبتة قبل الإصلاح تحتاج حذفاً أخيراً واحداً). لو نُشر مستقبلاً على متجر رسمي فيجب توليد مفتاح نشر خاص يبقى سرياً. **إصلاح سبتمبر 2026:** الـ p12 الأول كان يحمل شهادة بحدود `subject`/`issuer` فارغتين (dname فارغ عند التوليد) فرفضها JDK 17 (`Empty issuer DN not allowed`) وفشل `assembleDebug` — أُعيد توليده عبر keytool بمعرّف DN صالح (`CN=qabas-ci, OU=Qabas Studio, O=Qabas, C=SA`) بنفس المواصفات (PKCS12، alias `androiddebugkey`، كلمة المرور `android`)، مثبَّت في الـ repo حتى تعمل Actions بلا أسرار.
- **اختبارات:** `StyleBrainTest` (عقل الأساليب) · Robolectric unit tests · Roborazzi screenshots · `ExampleInstrumentedTest`.

---

## 8. لوحة المطور — أرقام حقيقية فقط

- `ApiUsageTracker` يلتفّ حول **14 نقطة HTTP حقيقية**: Gemini ×6، Groq، Azure TTS، ElevenLabs، HuggingFace، Pexels، Pixabay، Supabase/Firestore.
- المستخدمون والشحنات والقوائم: تحميل فعلي من Firestore ثم Supabase fallback.
- العدّ الأسبوعي للمشاريع/النصوص/الأحاديث: عدّ يومي حقيقي من Room عبر `countXBetween(start,end)`.
- رسائل صادقة بدل الأرقام المختلقة: «شاشات الزيارة» غير المُتتبَّعة تُعرض كمؤشرات فارغة، وليست أرقاماً مزيّفة.
- **بطاقات سريعة جديدة (سبتمبر 2026):** شبكة اللوحة أُضيفت إليها «فحص صحة المفاتيح الحي 🩺» (يقوم `LiveHealthCheckPanel` من `ApiKeysScreen.kt` بفحص 7 خدمات بأزمنة حقيقية) و«استهلاك API» (يفتح قسم الإحصائيات) ومؤشر حالة Supabase 🟢/🔴 في البطاقة الرئيسية بجانب حالة الإنترنت.
- **«المحرر» — وضع التشغيل الحقيقي (سبتمبر 2026):** تبويب «المحرر» صار بوضعين: **«تشغيل في التطبيق»** (افتراضي) يقرأ أمرك العربي وينفّذه فعلاً داخل التطبيق عبر `AiOperatorRouter` (`AiOperatorEngine.kt`) بأولوية حتمية STATUS → DIAGNOSE → SCRIPT → CODE_HINT → CHAT: «افحص/طبيب/صحة» → `AppSelfDoctor.runFullDiagnosis` مع عرض `DoctorReport` الحقيقي، «سكريبت/فيديو عن …» → `AppServices.generateScript` والمشاهد الفعلية، «الحالة/الجهاز» → أرقام حقيقية (`StatFs`/`Runtime`/`BuildConfig`)، «الإعدادات/المشاريع/القرآن…» → تنقّل فعلي داخل التطبيق، وإلا محادثة مجانية عبر `PollinationsTextService` (بلا مفتاح) — **بلا نجاح وهمي**؛ و**«تحرير الشيفرة»** (مسار GitHub → CI → APK) كما هو دون تأثر.

---

## 9. الوضع الحالي والخطوة التالية

- **مُحصَّن في الكود:** مسار الإنتاج (كابشن عربي، توقيتات آمنة، دمج xfade حقيقي، فلاتر LUT، Audio Ducking، عزل كابشن إلزامي، StyleDirective مطبّق فعلياً) · العقل الأسلوبي بلا أساليب مزيّفة · لوحة مطور خالية من البيانات المزيّفة (سجل الانهيارات بتشخيص ثلاثي) · سبلاش «الكتاب المفتوح» كان بإنتاج Compose نقي ثم **أُزيل كلياً (سبتمبر 2026)** وأيقونة الـ Launcher حُيّدت بمظهر ذهبي محايد لانتظار اللوجو الجديد · Supabase مفعّل · CI أخضر · **بناء محلي أخضر + حارس الانهيارات `QabasCrashGuard`**.
- **الأولوية المعلّقة:** **إثبات التصدير على جهاز حقيقي** — تثبيت APK → فكرة عربية قصيرة → معالجة → تصدير → تشغيل الملف (تحقق: كابشن عربي، مدة، عمل بلا نت/بلا TTS). أي انهيار الآن يُسجَّل تلقائياً في `crash_logs/` وتظهره لوحة المطور بتشخيص عربي؛ وعند استلام اللوجو الجديد (PNG ≥1024 بخلفية شفافة أو SVG) يُطبَّق على أيقونة الـ Launcher ولوجو شاشة التحميل `assets/logo/p0..p4.txt`؛ وتحلقة التنقل الشمسية تُتحقق (حوار تأكيد ← تنقّل، تثبيت 270°)؛ وقسم القرآن (Hub المصحف الذهبي) يُتحقق كما في `AGENTS.md` §4 — منها **النص العثماني الحقيقي** (أي سورة من «المصحف الشريف 📖» تُظهر الآيات بدل «النص غير متوفر») و**البحث النصي** («الكرسي» → آية 2:255 تفتح بتظليل ذهبي) و**التفسير الميسر المدمج** (تفسير أي آية، ومنها 2:255 و1:1 و114:6).
- **رتبة «مطور» لبريد المالك (سبتمبر 2026):** حساب المالك يُرفَع تلقائياً لرتبة مطور (استثناء وحيد بطلب المالك، بلا منح بالبريد لبقية الحسابات)، ولا تُخفَّض رتبته بمزامنة Firebase مهما كان الـ claim.
- **إصلاح تعطل الإنتاج (سبتمبر 2026):** مهلة محرك الإنتاج رُفعت إلى 180–420ث حسب عدد المشاهد (كانت تخنق الترميز متعدد المراحل)، والدمج صار يفلتر المشاهد الصالحة ويرتد لدمج مباشر بلا انتقالات عند فشل الدمج السينمائي، والإطار المحلي لا يعيد ملفاً فاشلاً بصمت (إعادة محاولة + حذف صادق)؛ كما أُصلح خطأ التجميع الأخير (قوس `object Auth` المحذوف في `CloudServices.kt` + استيرادات `AccountSettingsSection.kt`).
- **لتفعيل السحابة في الـ APK الموزّع عبر Actions:** أضف `SUPABASE_URL` و`SUPABASE_ANON_KEY` (اختيارياً `GEMINI_API_KEY`) في **GitHub Secrets** — الخطوة جاهزة في الـ workflow.
- **تسليم اللوجو الجديد (سبتمبر 2026):** السبلاش أُزيل كلياً والأيقونة حُيّدت بمظهر ذهبي محايد بانتظار الملف. عند الاستلام: استبدال `drawable/ic_launcher_foreground_neutral.xml` (و`mipmap/ic_launcher*.xml`) بوضع PNG بديل، وإعادة توليد `assets/logo/p0..p4.txt` (Base64) للوجو شاشة التحميل `DataLoadingScreen`.

---

## 10. سجل مختصر (ما وَثّقه المساعدون سابقاً — ليس دليلاً)

- **أغسطس 2026:** تحصين مسار الإنتاج و`StyleBrain` ضد النجاح الوهمي؛ تحرير التركيب والاعتماد على البيانات الحقيقية في لوحة المطور؛ آية الدخول والسبلاش؛ إزالة صور وهمية من الشاشات.
- **سبتمبر 2026:** Gemini ↑ إلى 2.0-flash؛ دمج FFmpeg حقيقي (`mergeVideo` عبر xfade)؛ تعليق صوتي مجاني دائماً (TTS النظام)؛ شاشة مفاتيح كاملة للطبقة المجانية (Azure + ElevenLabs + بطاقة «يعمل بلا مفاتيح»)؛ انتقالات `AnimatedContent` بين الشاشات؛ مكوّنا `QabasCard`/`QabasSectionHeader`؛ حماية `SupabaseConfig` ضد placeholder؛ خطوة `Inject secrets` في CI.
- **سبتمبر 2026 (تنظيف):** حذف سكربتات التصحيح وآثار التفكيك (`jadx_dir/`, `decompiled_apk/`, ملفات patch/scripts) من جذر المشروع؛ إعادة كتابة `README.md` كتوثيق حقيقي للمنتج بدل سجلّ التغييرات.
- **سبتمبر 2026 (بناء + حارس):** تجهيز بيئة بناء محلية كاملة (JDK 17 Temurin + SDK 36 في `~/tooling`) وإثبات `assembleDebug` أخضر محلياً؛ إضافة `QabasCrashGuard` العالمي (التقاط/توثيق/استعادة الانهيارات) وتركيبه في `QabasApplication`.
- **سبتمبر 2026 (شاشة المفاتيح):** حصاد شبه آلي (التقاط/تحقق تلقائي من الحافظة + زرّا «التقاط من الحافظة»/«افتح الموقع»)، شارة «🆓 بديل مجاني» لكل بطاقة، ولوح «فحص صحة المفاتيح الحي 🩺» لـ 7 خدمات — 8 نقاط تحقق ملفوفة بـ `ApiUsageTracker`. **إصلاح:** أزرار الحقل الفارغ خرجت من `trailingIcon` (شكل باركود غير قابل للضغط) إلى صفّ أسفل الحقل، و`ApiKeyValidator` يرفض مبكراً أي مفتاح Gemini لا يبدأ بـ `AIzaSy…` برسالة عربية واضحة (يكشف توكنات `AQ.…`).
- **سبتمبر 2026 (هوية الإنتاج AI):** خلفيات برمجية مولّدة (`ProceduralBackdropEngine`, CI `1c8ba7d`) لمسار الاستوديو بدل الصور الوهمية؛ نظام ألوان بنفسجي مهيمن + إكسنت سيان (`f5bcdec`) مع حفظ الذهبي للمقدسات؛ شاشة دخول بدمج اللوجو الرسمي مع الترحيب (`bd00524`).
- **سبتمبر 2026 (سجل الانهيارات + آخر البيانات المزيفة + سبلاش «الكتاب المفتوح»):** قسم `CrashLogsSection` في لوحة المطور يقرأ `crash_logs/*.txt` فعلياً مع تشخيص عربي ثلاثي لكل انهيار (لماذا ‽ / كيف حدث؟ / الحل ✓ — 10 فئات معروفة) وأزرار تحديث/مسح فعليين؛ حذف بذرة القرارات الملفّقة من `QabasBrainRepository` (أصبحت `emptyList()`)، وعدّاد «المستخدمين» صار فعلياً `devUsers.size` من `Database.getAllUsers()`؛ وإعادة كتابة `SplashScreen.kt` كإنتاج Compose نقي (بلا Lottie/صور): كتاب يفتح بحركة `rotationY` + `cameraDistance` حول العمود الفقري المذهّب، صفحات فيها «۞» وآية الطمأنينة، ثم شعلة «✦ قَـبَـسْ» (Amiri) بتوهّج ذهبي وحركة نابضة، وآية الدعوة، مع تخطي/لمسة/مهلة أمان 4.5ث — البناء المحلي `:app:assembleDebug` أخضر (2m19s).
- **سبتمبر 2026 (قسم القرآن «المصحف الذهبي Hub»):** إعادة هيكلة `QuranTajweedScreen.kt` من 4 تبويبات إلى **Hub من 5 أقسام** بشريط علوي: المصحف الشريف 📖 (القارئ + بطاقة «متابعة القراءة») / القراء والروايات 🎙️ (12 قارئاً حقيقياً بسيرهم + 10 روايات) / التفسير والمصادر 📚 (9 مصادر معتمدة بأسماء المؤلفين) / الأذكار 🤲 (صباح/مساء بنصوص ثابتة وعداد يدوي) / أكاديمية التجويد 🎓 (مراحل/اختبار/موسوعة كتبويبات داخلية محفوظة). حفظ دائم في `qabas_prefs`: آخر سورة مقروءة + القارئ + الرواية + علامة الفاصل 🔖. كل الحالات صادقة بلا بيانات مزيفة — زر الصوت في القارئ يعرض «قيد التجهيز»، ومصادر التفسير نصوصها الكاملة قيد التجهيز بلا تفسير غير موثوق — البناء المحلي `:app:assembleDebug` أخضر (2m9s).
- **سبتمبر 2026 (النص القرآني الحقيقي + مقارنة المصحف الذهبي):** دمج **نص حفص العثماني الكامل** (Tanzil — مطبوعات مجمع الملك فهد) في `assets/quran/uthmani.json` (~1.97MB) مُنزَّلاً برمجياً بحتاً من `fawazahmed0/quran-api` بلا حرف مكتوب يدوياً — 114 سورة و6236 آية مُتحقق منها، بنية `{"quran":[...]}` تُطابق `parseQuranJson` بلا أي تعديل، والقارئ يعرض الآيات الحقيقية (رأس البسملة إلا التوبة، خط Amiri، ألوان التجويد، وضع التسميع، «إنشاء ريلز من الآية» بتمرير النص الحقيقي) دون تغيير سطر عرض واحد. **مقارنة بالمصحف الذهبي:** عندنا النص + الأذكار + قوائم القراء/التفاسير + الأكاديمية؛ الفجوات الموثقة (تُنفَّذ لاحقاً بخطوة واحدة): تفسير/إعراب/بلاغة/معنى كلمة على مستوى الآية، استماع حقيقي (40+ قارئاً)، تتبع الختمة/الأحزاب، أوقات الصلاة والقبلة — البناء المحلي `:app:assembleDebug` أخضر.
- **سبتمبر 2026 (البحث النصي في القرآن + فتح الآية بتظليل):** `QuranDataProvider.searchVerses(query, limit=50)` بحث حقيقي في **6236 آية عثمانية** بمسارين `contains` (النص كما هو + نسخة مطبَّعة عبر `normalizeArabicText`: تجريد التشكيل/التطويل وتوحيد الهمزات أ/إ/آ/ٱ→ا و ى→ي و ة→ه) مع **فكّ «ال» التعريف** («الكرسي» ← «كرسيه» ← آية الكرسي 2:255)؛ قسم «نتائج البحث في الآيات ✨» أعلى قائمة السور (بطاقات قابلة للنقر، فاصل «السور المطابقة»، حالة فارغة صادقة)؛ وفتح الآية في المصحف بتمرير تلقائي (`LazyListState` + `animateScrollToItem` متوافق مع إزاحة البسملة) وتظليل ذهبي (حد `2.dp` + خلفية `#1E293B`) مع حفظ `last_read_surah` وتصفير الهدف عند الخروج — البناء المحلي أخضر (1m28s).
- **سبتمبر 2026 (التفسير الميسر المدمج):** دمج **نص التفسير الميسر** (مجمع الملك فهد لطباعة المصحف الشريف) في `assets/quran/tafsir_muyassar.json` (~2.68MB) مُنزَّلاً برمجياً بحتاً من `Nasaq-GP/Quran_tafsir` (6236 صفاً، لا حرف مكتوب يدوياً، تفسير آية الكرسي 2:255 مطابق للمطبوع) ببنية مقصوصة `{"tafsir":[{"s":سورة,"a":آية,"t":النص}]}` غلافها التام 1:1..114:6. `QuranDataProvider`: `loadTafsirFromAssets` (كسول بفحص مسارين) + `parseTafsirJson` (بدعم بدائل {surah,ayah,text} لمصادر أخرى مستقبلاً) + `getTafsirForVerse` بمفتاح «سورة:آية» يُرجع null عند الغياب؛ و`getVersesForSurah`/`searchVerses` تملآن `tafseer` الحقيقي بدل الثابت الفارغ. حوار التفسير في القرّاء يعرض النص الحقيقي والقطرة الاحتياطية صادقة («لا يتوفر تفسير ميسر لهذه الآية في النسخة المدمجة حالياً.» بدل النص الترويجي الملفّق)، وبطاقة الميسر في «التفسير والمصادر 📚» حملت شارة «مدمج ✓ متاح الآن في المصحف» (بقية المصادر قيد التجهيز بلا تفسير غير موثوق). بذلك اكتملت **الخطوة الأولى** من فجوات مقارنة المصحف الذهبي — البناء المحلي `:app:assembleDebug` أخضر (1m22s).
- **سبتمبر 2026 (قارئ المصحف بالصفحات — تصميم أصلي):** `MushafReaderScreen.kt` + `MushafPageData.kt` + `assets/quran/madani_pages.json` (49KB: حدود 604 صفحات + بدايات الأجزاء الـ 30، مولّدة برمجياً من `fawazahmed0/quran-api` تحت رخصة Unlicense — ملكية عامة تسمح بالاستخدام التجاري — **لا كود ولا موارد من quran_android الـ GPL-3**). HorizontalPager ب Donner RTL، شريط علوي (سورة/جزء) وسفلي (رقم صفحة/تنقل/🔖/اختيار)، نافذة سورة/جزء/صفحة/علاماتي، حفظ `last_read_page` + `mushaf_bookmarks` في `qabas_prefs`، والنصوص من `uthmani.json` المحلي حصراً. الدخول: بطاقة «📖 المصحف بالصفحات» في قسم المصحف الشريف. **مطابقة صور النماذج وظيفياً:** بطاقة «فاصل الصفحة» أعلى السور + رقم الصفحة بجانب كل سورة + الحزب (60 حزباً من `api.quran.com` v4 العام) في الشريط العلوي + ورقة سلايدر صفحات سفلية (Slider + شريط الجيران) بالنقر على رقم الصفحة — كلها بهوية قبس الأصلية لا نسخاً. **التلاوة الصوتية (Quran Foundation — المشعّر Alafasy):** `QuranAudioService` (توكن OAuth2 + آيات الصفحة mp3) + `QuranAudioPlayer` (Media3) بزر 🎧 في الورقة السفلية (تحميل/تشغيل/إيقاف، ويتوقف عند تغيير الصفحة) — المفاتيح من `.env`/`BuildConfig` فقط (`QF_CLIENT_ID`/`QF_CLIENT_SECRET` في `.env.example` وحقن CI)، وبدونها رسالة صادقة بلا وهم.
- **سبتمبر 2026 (إزالة السبلاش كلياً + حياد أيقونة الـ Launcher — استقبال اللوجو الجديد):** بطلب من المالك («ازله كليا»): أُزيلت شاشة السبلاش من كل مكان (كود/موارد/ثيم) — `MainActivity.kt` يبدأ الآن من `AppState.DATA_LOADING` بلا `installSplashScreen`، والثيم في `AndroidManifest.xml` صار `@style/Theme.MyApplication`، وحُذفت `SplashScreen.kt` و`values/themes_splash.xml` و`values/colors_splash.xml` و`drawable/ic_qabas_splash_icon.xml` مع كل أصول اللوجو القديمة (`qabas_logo.webp`, `qabas_logo_pro.xml`, `qabas_app_icon_*.jpg`, `qabas_logo_*.jpg`, `ic_qabas_foreground.png`, `ic_launcher_background.xml`, `ic_launcher_foreground.xml`, وجميع `ic_launcher*.webp` في الكثافات الخمس، مع إفراغ مجلداتها). **أيقونة محايدة:** `drawable/ic_launcher_foreground_neutral.xml` (حلقة + نجمة ٨ رؤوس ذهبي `#D4AF37` على شفاف) استُعملت foreground/monochrome في ملفّي `mipmap-anydpi-v26`، وأُنشئ `mipmap/ic_launcher.xml` + `ic_launcher_round.xml` (vector 108dp: خلفية `#030508` + رمز ذهبي) كـ fallback لـ API 24–25، مع إبقاء `ic_launcher_background_qabas.xml`. حُذفت كتلة اللوجو من `LoginScreen.kt` (مع استيرادات `Image/ContentScale/painterResource`). تحقق grep شامل على `app/src` = **صفر مطابقات** لكل الأنماط المحذوفة. `assets/logo/p0..p4.txt` (Base64) و`DataLoadingScreen.kt` باقيان كموقعَيّ لوجو شاشة التحميل في انتظار اللوجو الجديد.
- **سبتمبر 2026 (حلقة التنقل الشمسية):** الشريط السفلي استُبدل بلا أي حذف بنظام «الكواكب حول الشمس» `QabasSolarSystemNavigation.kt` (جديد): 5 كواكب بنفس ألوان/ترتيب الأقسام تدور 8°/ث حول شعلة ذهبية مع تثبيت الكوكب النشط عند 270° وتكبير/توهج بحد ذهبي (الحركة عبر `graphicsLayer` فقط، تجميد عند غير `RESUMED` أو فتح حوار، احترام `ANIMATOR_DURATION_SCALE`)، ونقرة كوكب → حوار تأكيد («الدخول إلى …؟»/«تأكيد ✓»/«إلغاء») → تنقّل مع اهتزاز — `QabasBottomNavigation` باقٍ دون استخدام. إصلاح بناء موثَّق من فحص AAR: `compose.ui` المحلول 1.9.0 لا يحوي حزمة `androidx.compose.ui.haptics`؛ `HapticFeedbackType` في `androidx.compose.ui.hapticfeedback` و`LocalHapticFeedback` في `androidx.compose.ui.platform` — البناء المحلي `:app:assembleDebug` أخضر (1m12s).
- **سبتمبر 2026 (طبقة OpenAI الاختيارية):** محادثة حقيقية عبر `gpt-4o-mini` (`RealOpenAIService.kt`) بأولوية fallback آمنة **OpenAI → Groq → Gemini** في `chatWithAssistant` و`executeShortTaskWithFallback` — مفتاح `sk-proj-…` يُقرأ من `qabas_prefs` ثم BuildConfig، مع بطاقة في شاشة المفاتيح + تحقق حقيقي (`ApiKeyValidator`) + تصدير/استيراد في نسخ الـ JSON + لفّ `ApiUsageTracker` (يظهر في لوحة المطور) + إدخال «مفاتيح API 🔑» في شبكة اللوحة كقسم داخلي بلا مغادرة. `.env.example` بـ `OPENAI_API_KEY="your_key"`؛ **القاعدة الأمنية:** لا مفتاح حقيقي في الكود/ملف مقتَفَع (يُستخرج من الـ APK) — المفتاح الحقيقي في `.env` المحلي (مستثنى git) أو GitHub Secret.
- **سبتمبر 2026 (إغلاق صلاحيات البريد — الأعلام فقط):** أُزيلت كل صلاحيات البريد المخزَّنة يدوياً (`aly750834`/`aliwalead`/`xman88371`/`admin@qabas`/`family@qabas`/`friend@qabas`/`peeesa7`) من `AccountService.kt`/`LoginScreen.kt`/`AuthScreens.kt`/`SettingsScreen.kt`/`RequestChatScreen.kt`/`LeagueService.kt`؛ admin/قريب/مطوّر صارت **أعلام `qabas_prefs` فقط** (`is_admin`/`is_relative`/`is_developer`). `is_developer` يُكتب الآن فقط لحساب المالك عند التسجيل/الدخول (`CloudServices.isOwnerAccount`) — لا يُمنح وضع المطور افتراضياً ولا لأي حساب آخر؛ الأجهزة القديمة المخوَّلة تحتفظ بعلامتها المكتوبة. التسجيل/الدخول يكتبان العلامات فقط ولا يمنحان صلاحية حسب البريد. تحقق grep على `app/src/main`: صفر مطابقات لبقايا الأنماط ومن يتبقَّى `aliwalead` فهي تسمية عرض في لوحة المطور فقط (`DeveloperDashboardScreen.kt:449`) لا تمنح صلاحية.
- **سبتمبر 2026 (توحيد الحزمة على `com.qabas.app`):** حزمة الجذر كانت `com.example` بينما `namespace`/`applicationId` = `com.qabas.app` (تعارض فئة `R` المولَّدة يهدم البناء). نُقلت الشيفرة بـ `git mv`: `app/src/main/java/com/example` (132 ملف Kotlin + `ui/theme` + `ui/input`)، `app/src/test/java/com/example`، `app/src/androidTest/java/com/example` → `com/qabas/app`، مع sed شامل `^package/^import com.example` → `com.qabas.app` (التوزيع: 123 × `com.qabas.app` + 6 × `ui.input` + 3 × `ui.theme`)، وإصلاح فلتر الـ crash stack في `DeveloperDashboardScreen.kt` (`at com.qabas.app.`) وتشخيص `ExampleInstrumentedTest.kt` (`"com.qabas.app"`)، وحذف ملف الشظية `AppNavigation_PROCESSING_snippet.txt`. تحقق grep = صفر بقايا (الوحيد المتبقي `.beads/issues.jsonl` — تصدير تاريخي سلبي لا يُعدَّل). **تحقق البناء مُعلَّق** لغياب JDK/SDK في بيئة هذه الجلسة — يُثبَّت `assembleDebug` على جهاز مؤهَّل قبل الإغلاق.
- **سبتمبر 2026 (إصلاح انهيار FlowRow):** `NoSuchMethodError` على `FlowRow` في شاشات الملف الشخصي/التجويد/الفهم/الهاشتاقات بسبب عدم تطابق `foundation-layout` (الـ BOM 2024.09.00 يجلبه تعدياً بينما `compose.ui` انحل لـ 1.9.0). الحل: تثبيت `foundation` و`foundation-layout` صراحةً على 1.9.0 + ترقية `versionCode` إلى 3 / `versionName` 1.2.1 — البناء عبر CI وإعادة التثبيت يزيل الانهيار.
- **سبتمبر 2026 (تحليل شامل + إصلاحات + 5 تطويرات متقدمة):** تحليل شاشة بشاشة كشف 5 عيوب حرج/متوسط (تسجيل خروج يمسح كل المفاتيح، `BrainStatus.SUCCESS` عند الفشل، XP وهمي، تحليل صوتي مزيّف، عدم `FirebaseAuth.signOut()`) — أُصلحت جميعها. ثم 5 تطويرات في مسار الإنتاج: (1) معالجة توازية ذكية (semaphore = 2/3/4 حسب أنوية الجهاز، مُفعّلة افتراضياً)، (2) حفظ المشاهد كـ JSON في prefs بعد التوليد + مسح بعد النجاح (لا إعادة توليد Gemini عند فشل FFmpeg)، (3) إعادة محاولة تحميل B-Roll بـ 3 محاولات بتأخير تصاعدي (1.5ث → 3ث)، (4) كاش ذكي (حذف الأقدم من ساعة بدلاً من كل شيء)، (5) فحص مساحة التخزين (`< 200MB` = فشل، `< 500MB` = تحذير) قبل بدء المعالجة.
- **سبتمبر 2026 («المحرر» — وضع التشغيل الحقيقي):** تبويب «المحرر» صار بوضعين مع **تنقّل فعلي** بين الأقسام (الإعدادات/الملف الشخصي/المشاريع/القرآن…). السكربت والمحادثة يمرّران أولاً عبر `PollinationsTextService` — **نموذج نصي بلا مفتاح** (بدون API) — قبل الخدمات السحابية. «تحرير الشيفرة» لم يتأثر.
- **سبتمبر 2026 (زر «فتح الموقع» في شاشة المفاتيح):** أزرار الروابط تعمل الآن عبر `openUrl` (متصفح افتراضي ← بدائل متصفحات شهيرة ← عرض الرابط نصاً عند غياب أي متصفح) + `<queries>` صريحة في Manifest لرؤية المتصفحات.
- **سبتمبر 2026 (لوحة تحكم سحابية كاملة):** لوحة المطور أصبحت لوحة تحكم: حارس وصول (المالك فقط)، تحكم بعيد عبر Firestore (صيانة/طلبات/رد آلي/رسالة)، سجل تدقيق، إدارة مستخدمين متقدمة (فلترة/تأكيد/CSV)، إشعارات سحابية، نسخ احتياطي واسترجاع JSON فعلي (معاينة + تأكيد + دمج)، ورسوم إيرادات شهرية. **إصلاح أمني تالٍ:** إغلاق ثغرة `is_developer` من مسارين — التسجيل/الدخول يكتبان العلم لحساب المالك فقط (`CloudServices.isOwnerAccount`)، و`AccountService.isDeveloperOrAdmin` يقرأه بافتراض `false` — لا يُمنح وضع المطور افتراضياً لأي حساب أو جهاز.


### تحديث سبتمبر 2026: "المصحف الذهبي" داخل قسم القرآن
أضيفت الميزات التالية إلى قسم القرآن:
- عرض المصحف بصفحات مطبوعة (٦٠٤ صفحة) قابلة للقلب والتصفح
- آية اليوم المختارة تلقائياً بناءً على يوم السنة
- متابعة القراءة المحفوظة بين الجلسات
- أذكار الصباح والمساء والأدبار مع العداد اليومي
- سبعة تبويبات في Hub: المصحف الشريف، القراء والروايات، التفسير والمصادر، الأذكار، أكاديمية التجويد، الصفحات الذهبية، المكتبة والدراسة
- البحث النصي في الآيات مع التظليل الذهبي عند الفتح
- التفسير الميسر الحقيقي (مجمع الملك فهد) للآيات
- مواضيع القرآن (٤٢ موضوعاً) وتخريج الحديث (٥٠ حديثاً) في شاشة المكتبة والدراسة
- مواقيت الصلاة وبوصلة القبلة عبر OpenStreetMap (مجاني بلا مفتاح)
- المكتبة العامة بمختارات من المصادر الإسلامية

جميع الميزات تعمل محلياً مع خيار الاتصال الشبكي للمطالبات الصوتية والترجمات بحسب التوفر.
